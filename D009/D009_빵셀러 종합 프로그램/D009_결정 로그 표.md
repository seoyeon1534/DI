| 단계 | 액션 | 근거 | 결과 |
|---|---|---|---|
| 1. 중복 제거 | 완전 중복 행 drop | 동일 키 동일 값은 시스템 입력 오류로 간주 | 312건 제거 |
| 2. 문자열 정제 | store_id를 strip | 'S01 '과 같은 서로 다른 공백은 표기에 혼란 | 앞뒤 공백 제거 |
| 3. 날짜 파싱 | date_str을 datetime으로 | 월별 집계가 분석 목적이므로 datetime 필요 | NaT(파싱 실패): 0건 |
| 4. 이상치 처리 | 음수 revenue 분리 | 매출 분석이 목적이므로 환불은 별도 보관 | 환불 1건 |
| 5. 결측치 처리 | revenue 결측 → 재계산으로 복구 | quantity와 unit_price는 오염되지 않았으므로 |  revenue가 NaN인 60건은 재 계산 후 그래도 남은 결측이 있다면 그때 제거 |